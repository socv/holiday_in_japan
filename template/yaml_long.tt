[% FOREACH row IN rows %]
- { [%+ %]
  [% FOREACH k IN standard_all_keys %]
    [% NEXT UNLESS row.defined(k) %]
    [% ", " UNLESS loop.first %]
    [% k %]: "[%+ row.$k %]"
  [% END %]
[% +%] }
[%+ END %]
